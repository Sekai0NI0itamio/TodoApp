## AdvancedTodo v1.3

- Reworked the blocker settings into a sidebar-driven layout with expandable blocker navigation.
- Added a connected "General Blocker" page that renders the blocker sections in one scroll flow and updates the sidebar highlight as you move through it.
- Folded the old floating shared blocker area back into the blocker categories so the shared lists stay accessible in-context.
- Added add-item prompts that can mirror reminder entries into auto-close and auto-close entries back into reminder.
- Fixed the accessibility-permission restart path so the app can relaunch itself after permission is granted.
- Fixed staged update install relaunching so the replacement app opens again after the old process exits.
